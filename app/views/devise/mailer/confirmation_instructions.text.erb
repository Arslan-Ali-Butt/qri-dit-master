Hello <%=@resource.name%>!
You can confirm your new email through the link below:
<%=confirmation_url(@resource, confirmation_token: @token, subdomain: Admin::Tenant.find(Apartment::Database.current_tenant.scan( /\d+$/ ).first).subdomain)%>
