- callToActionUrl = unlock_url(@resource, unlock_token: @token, subdomain: Admin::Tenant.find(Apartment::Tenant.current_tenant.scan( /\d+$/ ).first).subdomain)

- content_for(:header, "Unlock Your Account")

%p
  Hello #{@resource.name},
%p Oops, your account has been locked due to an excessive number of unsuccessful login attempts. 
%p Click the link below to unlock your account.


= render "layouts/emailbrand/call_to_action_button", :line1 => "Click here to", :line2 => "Unlock Your Account", :line3 => "", :linkUrl => callToActionUrl
