.row.reportphoto-collection
  = render partial: 'tenant/report_photos/resource', collection: @resource.photos.limit(8)
.row
  .col-sm-12
    - if @resource.photos.count > 8
      = link_to 'View All', tenant_report_photos_url(report_id: @resource.id)
