.modal-dialog.modal-lg
  .modal-content
    .modal-header
      %div.close
        %button{style: 'border: none; background: transparent; color: white;', 'aria-hidden' => 'true', 'data-dismiss' => 'modal', type: 'button'} &times;
      %h4.modal-title #{@resource.name} Checklist Preview
    .modal-body
      .box
        .box-header
          %h2
            %i.checklist-icon
            %span.break
              Checklist Preview
          .box-icon
            %a.btn-minimize
              %i.fa.fa-chevron-up
        .box-content.qrid-content
          %p Preview and test your checklist to get a feel for exactly how a reporter in the field will see and use it.
          
          - if params[:permatask].present?
            = render 'trial_report'
          - else
            = render 'trial_select'
    .modal-footer
      %button.btn.btn-default{'data-dismiss' => 'modal', type: 'button'} Close
